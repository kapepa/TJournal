import React, {FC} from "react";
import style from './style.module.scss';

const SubscribePanel: FC = () => {
  return (
    <div className={`${style.subscribe_panel}`}>
      SubscribePanel
    </div>
  )
};

export default SubscribePanel;